export default {
  app: Object.freeze({
    path: '/*',
  }),
  employees: Object.freeze({ path: '/employees' }),
  interns: Object.freeze({ path: '/interns' }),
  absences: Object.freeze({ path: '/holidays' }),
  loanManagement: Object.freeze({
    path: '/loanManagement',
  }),
  documents: Object.freeze({ path: '/documents' }),
};

